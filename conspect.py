# Конкурентность (concurrency) - запуск на выполнение сразу нескольких задач (не обязательно в 1 момент времени
# выполняется несколько). Зависит от ПО.


# Параллельность (parallel) - конкурентность, когда 2+ задачи выполняются одновременно. Зависит от железа.

# thread-safe - потокобезопасность, означает что при работе с объектом не возникают известные проблемы при работе с конкурентностью
# GIL (Global Interpreter Lock) - глобальная блокировка интерпретатора


# Задачи могут быть:
# CPU-bound - зависит от мощности процессора
# IO-bound - зависит от системы ввода/вывода


# threading - IO-bound задачи (новый поток)
# asyncio - IO-bound задачи(1 поток с конкуренстностью за ресурсы)
# multiprocessing - любые задачи(новый процессор с потоком)
_--------------------------------------------------------------

# asyncio - асинхронное выполнение, подходит для IO-bound задач, работает ровно 1 поток
# Плюсы:
# + скорость и экономия времени, вместо x + y + z -> max(x, y, z)
# + управляемость
# + меньше потребление ресурсов
# Минусы:
# - "умирает" из-за одного блокирующего вызова (!)


# 1) корутина работает как генератор
# 2) async - явный флаг, что данная функция является асинхронной (корутиной)
# 3) await - явный флаг, что в это месте функция встает на паузу и дает работать другим, пока ждет свои данные
# 4) event loop - цикл событий, механизм, который отвечает за планирование и запуск корутин. Можно представить как
# список/очередь, из которого в вечном цикле достаются и запускаются корутины


# Частые ошибки:
# - не использование await внутри корутины
# - создание корутины, но использование ее, как функции
# - использование внутри корутин синхронного(блокирующего) кода, в том числе IO


import asyncio


async def coroutine_1():
    print("Корутина 1: старт")
    await asyncio.sleep(2)  # имитация IO (запрос к БД, API и т.д.)
    print("Корутина 1: финиш (через 2 сек)")
    return "результат 1"


async def coroutine_2():
    print("Корутина 2: старт")
    await asyncio.sleep(1)
    print("Корутина 2: финиш (через 1 сек)")
    return "результат 2"


async def coroutine_3():
    print("Корутина 3: старт")
    await asyncio.sleep(3)
    print("Корутина 3: финиш (через 3 сек)")
    return "результат 3"


async def coroutine_4():
    print("Корутина 4: старт")
    await asyncio.sleep(1.5)
    print("Корутина 4: финиш (через 1.5 сек)")
    return "результат 4"


async def main():
    # asyncio.gather() запускает все корутины конкурентно в event loop
    results = await asyncio.gather(
        coroutine_1(),
        coroutine_2(),
        coroutine_3(),
        coroutine_4(),
    )
    print("\nВсе результаты:", results)


# asyncio.run() создает event loop, запускает main() и закрывает loop
asyncio.run(main())